$(function() {
  userRouter = new Agreatfirstdate.Routers.UserRouter({profile: <%= raw(@profile.to_json(scope: :self)) %>});
  pillarsRouter = new Agreatfirstdate.Routers.PillarsRouter({pillars: <%= raw(@pillars.to_json scope: :self) %>, pillarCategories: <%= raw(@pillar_categories.to_json(scope: :self)) %>});
});