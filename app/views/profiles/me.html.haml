#event_photo_form.hidden
  = form_for EventPhoto.new, remote: true do |f|
    = hidden_field_tag :pillar_id
    = f.label :image, t('event_descriptors.any_pics')
    = f.file_field :image, class: 'event_photo_image_'
    %span.upload-status_ Select file
  .event_photos_previews_
#profile_avatars_form.hidden
  = form_for @profile, url: update_profile_path, method: :put, remote: true, html: {id: 'edit_photo'} do |f|
    = file_field_tag 'profile[avatars_attributes][][image]'
    %span.errors_

#event_items_popup
#profile_popup

.row
  .span3.span_first_pillar.top_row
    %section#leftPillarArray
      #leftPillarContainer.standart-widget

  .span9    
    %section#rightPillarArray
      .row
        .span3
          %section#pillarAboutMe
            #pillarAboutMeContent
        .span3
          #pillarAboutMePhoto.standart-widget
        .span3
          #pillarAboutMeMeet

      %section#leftMiddleBottomContainer.row
        .span3.span_second_pillar.top_row
          %section#leftMiddlePillar.standart-widget
        .span6
          .row
            .span3
              %section#rightMiddlePillar.standart-widget
            .span3
              %section#rightPillar.standart-widget
          .row
            .span6
              %section#bottomPillar
                %h2 I would totally have also added these categories too

:javascript
  window.router = new Agreatfirstdate.Routers.PillarsRouter({pillars: #{@pillars.to_json}, pillarCategories: #{@pillar_categories.to_json}});
  window.userRouter = new Agreatfirstdate.Routers.UserRouter({user: #{@profile.to_json}});
  window.userRouter.show();
  Backbone.history.start();