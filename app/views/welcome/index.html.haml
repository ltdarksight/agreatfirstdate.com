#sectionWelcome
  #sectionLogin
    %h1 Already a member?
    = form_tag @user do
      %p
        = text_field_tag :username, params[:username]
      %p
        = password_field_tag :password
      %p
        = submit_tag "Welcome back"

